# java solution

class GfG
{
   
    public static int getNth(Node node, int ind)
    {
       //Your code here
       Node temp = node;
       for(int i = 0 ; i<ind -1 ; i++){
           temp = temp.next;
       }
       return temp.data;
    }
}
